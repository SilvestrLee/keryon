Small adjustment before approval.

Please wrap addGlobalScope itself in a console check.

Use:

if (! app()->runningInConsole()) {

    static::addGlobalScope('church_tenant', function (Builder $query) {

        if (
            Auth::check()
            && Auth::user()?->church_id
        ) {

            $query->where(
                $query->getModel()->getTable() . '.church_id',
                Auth::user()->church_id
            );

        }

    });

}

Everything else looks good.

Regenerate BelongsToChurch.php and ask for approval again.

Do not run migrations.